const title         = '信心花舍'
const name          = 'vzt7'
const role          = 'Front-End Engineer'
const site          = 'https://vvzt.github.io/'
const yearsOfExp    = new Date()
const keywords      = ['vzt7', 'vvzt'].concat(name.split(' '), role.split(' ')).join(' ')
const description   = `Hey! I am ${name}, a ${role}.`;

export default { title, name, role, site, keywords, description, yearsOfExp }
